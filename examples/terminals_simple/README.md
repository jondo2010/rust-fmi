# terminals-simple

Minimal example that demonstrates `#[terminal]` integration without CAN or
`fmi-ls-bus`.

To bundle:

    cargo fmi bundle -p terminals-simple

The resulting FMU should contain:

    resources/terminalsAndIcons/terminalsAndIcons.xml
